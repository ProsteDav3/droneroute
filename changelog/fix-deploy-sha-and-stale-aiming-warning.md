- Fixed: deployed builds identify themselves again. The Dockerfile already accepted a commit SHA and the About dialog already shows one, but the deploy never passed it — so every build reported itself as "dev" and there was no way to tell whether a browser was running the new code or a cached service-worker shell.
- Added: uploading to DJI Cloud warns when the route tracks a camera target but contains no gimbal command at all. That combination means the waypoints were generated by an older cached build: the aircraft turns to the target correctly, but the gimbal holds its angle and the shot never focuses.
