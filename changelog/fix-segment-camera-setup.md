- Fixed: every exported segment now sets up its own camera — the gimbal angle for that leg, a one-second settle, and a focus — before it starts recording. A segment is flown as its own flight (a time-lapse series revisits the same route for months), but the opening gimbal command and the focus lived only on the parent route's first two waypoints, so every segment but the first took off with the gimbal wherever the pilot had left it and never focused. Routes that don't aim at a target (surveys, pilot-controlled camera) are untouched.
