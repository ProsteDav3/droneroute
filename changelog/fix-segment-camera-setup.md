- Fixed: the camera is set up before recording starts, so the shot is sharp from its first frame. On the opening waypoint the gimbal moves to that leg's angle, the aircraft holds for three seconds while it finishes turning to the target and the gimbal settles, then focuses — and only then does recording begin, so the pause never appears in the footage. Focusing after recording had already started (the previous arrangement) meant the opening of every clip was soft.
- Fixed: every exported segment now performs that setup itself. A segment is flown as its own flight — a time-lapse series revisits one leg per visit for months — but the opening gimbal command and the focus lived only on the parent route's first waypoints, so every segment but the first took off with the gimbal wherever the pilot had left it and never focused. Routes that don't aim at a target (surveys, pilot-controlled camera) are untouched.
