- Fixed: the lock/unlock buttons beside the waypoint range are now disabled while the range is unusable, and say why. An empty or out-of-mission range used to leave them looking live, and pressing one silently acted on whatever had been typed before — so the operator walked away believing they had locked a range they hadn't.
- Fixed: that range row now wraps instead of running off the edge of the sidebar on a phone. At 375px the sidebar is a ~318px drawer, and the part of the "Odemknout" button that escaped it sat on the drawer's own backdrop — tapping there closed the panel instead of unlocking anything.
