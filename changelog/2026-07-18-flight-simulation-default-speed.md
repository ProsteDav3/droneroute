### Changed

- Halved the flight simulation's playback pace (all speed options and the default) — smooth, continuous first-person motion reads as noticeably faster than the same nominal pace used to feel when it was rendered in choppy discrete jumps, so "1x" now moves at what used to be "0.5x". The relative 0.5x/1x/2x/4x multipliers are unchanged.
