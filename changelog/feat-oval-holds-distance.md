- Improved: the oval now holds a set distance **from the camera target** across the middle of the arc and opens out only near the two fixed ends, instead of blending the radius from the centre. That is what actually keeps the subject the same size: on the KCP orbit a 90 m hold turns 75–146 m into a steady 90 m, with the start and end still exactly where they were placed. The waypoints in between all move, rather than only the ones in the middle, and they are spaced evenly along the path instead of by bearing — no more crowding on the side where the path runs closest to the centre.
- Added: a red dashed ring around a locked building shows how far out the flight has to be for the **whole** building — width included — to fit in frame. The blue ring next to it is what its height alone needs.
- Improved: the template panel is shorter. The explanatory paragraph is collapsed behind a "Co tato šablona dělá" toggle (it stays open once you open it), and the warnings and hints are trimmed to a line each.
