- Added: waylines in the DJI Cloud wayline library panel can now be renamed in place — double-click a name or use the new pencil button, Enter confirms and Escape cancels. The new name is sanitized and length-fit the same way an uploaded mission name is, and an older DJI Cloud instance that doesn't have the rename endpoint yet shows a clear "can't do that" message instead of a generic failure.
